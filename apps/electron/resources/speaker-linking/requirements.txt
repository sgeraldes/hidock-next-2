# Python 3.11; install CUDA torch from the PyTorch index before this file.
# The RTX 4090 production path uses CUDA. CPU fallback works but is slower.
pyannote.audio>=4.0,<5
huggingface_hub>=0.34
onnx>=1.16
# DirectML runs the onnx-local voice engine on AMD, Intel and NVIDIA GPUs (Windows).
onnxruntime-directml>=1.20; sys_platform == "win32"
onnxruntime>=1.20; sys_platform != "win32"
